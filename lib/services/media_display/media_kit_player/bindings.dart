import 'package:get/get.dart';

import 'controller.dart';

class MediaKitPlayerBinding implements Bindings {
  @override
  void dependencies() {
    // Get.lazyPut<MediaKitPlayerController>(() => MediaKitPlayerController());
  }
}
